<% include partials/header %>
        
    <div class="message">
        <div class="text">
            <h1>Cards</h1>
            <h2><%= list %></h2>
            <!-- <ul>
    			<% cards.forEach(function(card) { %>
        			<li><%= card.name %> - <%= card.imageUrl %></li>
    			<% }); %>
			</ul> -->
        </div>
    </div>

<% include partials/footer %>
